# Copy this file to .env and fill in real values
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=bookstore_db
# Optional: override the default port
PORT=3000
# JWT secret for auth (set a strong secret in production)
JWT_SECRET=change_this_secret

# Mailtrap SMTP settings (example values for local testing)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_EMAIL=ec7315ca374065
SMTP_PASSWORD=3b33710cf7423c
SMTP_FROM_NAME=Bookverse
SMTP_FROM_EMAIL=no-reply@bookverse.local
SMTP_SECURE=false
SMTP_TLS_REJECT_UNAUTHORIZED=false
